# Repository Access

Remote name: `origin`

Remote URL:

```text
git@github.com:koril0620-sys/Capi-Rogue-final.git
```

Current branch: `main`

Common commands:

```bash
git status
git add .
git commit -m "message"
git push origin main
git pull origin main
```

Note: The Git remote is already configured in `.git/config` as `origin`.
